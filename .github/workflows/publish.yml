name: publish to nuget
on: workflow_dispatch

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Test
      run: dotnet test -c Release
    - name: Build
      run: dotnet build src/AtomReader/AtomReader.csproj -c Release
    - name: Package
      run: dotnet pack src/AtomReader/AtomReader.csproj -c Release -o .
    - name: Publish
      run: dotnet nuget push **/*.nupkg -s 'https://api.nuget.org/v3/index.json' -k "$NUGET_API_KEY"  --skip-duplicate
      env:
        NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
